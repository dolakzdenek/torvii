import * as React from "react";
import {
  TouchableOpacity,
  StyleSheet,
  GestureResponderEvent,
  Text,
  Pressable,
} from "react-native";

export interface WalletProps {
  id: string;
}

export function Wallet({ id }: WalletProps) {
  return null
}